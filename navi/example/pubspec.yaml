name: navi_example
description: Navi - Simple Example
publish_to: 'none'
version: 0.0.1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  navi: any

dev_dependencies:
  flutter_test:
    sdk: flutter

dependency_overrides:
  navi:
    path: ../

flutter:
  uses-material-design: true